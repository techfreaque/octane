module.exports = {
  printWidth: 80, // Wrap lines at 80 characters
  tabWidth: 2, // Use 2 spaces per indentation level
  useTabs: false, // Indent with spaces instead of tabs
  semi: true, // End statements with semicolons
  singleQuote: false, // Use single quotes instead of double quotes
  trailingComma: "all", // Add trailing commas wherever possible
  bracketSpacing: true, // Print spaces between brackets in object literals
  arrowParens: "always", // Always include parentheses around arrow function arguments
  endOfLine: "lf", // Use line feed (\n) as the line ending
  jsxSingleQuote: false, // Use single quotes in JSX
  proseWrap: "preserve", // Preserve the way prose is wrapped
  quoteProps: "consistent", // Only add quotes around object properties where required
};
